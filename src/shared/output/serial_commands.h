#pragma once
// We need to look at commands sent by consoles so we can make sure that they dont conflict.
enum SerialReports {
    REPORT_GET_HID,
    // PS3 controllers have a hid feature report that is required for identifying the controller.
    REPORT_GET_PS3_ID = 3,
    REPORT_GET_CONFIG,
    REPORT_SET_CONFIG
};
enum SerialCommands {
    COMMAND_REBOOT,
    COMMAND_GET_SIGNATURE,
    COMMAND_GET_CPU_FREQ,
    COMMAND_GET_BOARD,
    COMMAND_JUMP_BOOTLOADER_UNO,
    COMMAND_JUMP_BOOTLOADER,
    COMMAND_FIND_DIGITAL,
    COMMAND_FIND_ANALOG,
    COMMAND_FIND_STOP,
    COMMAND_GET_EXTENSION,
    COMMAND_AVRDUDE,
    DATA_INPUT_TYPE,
    DATA_SUB_TYPE,
    DATA_TILT_TYPE,
    DATA_MPU_6050_ORIENTATION,
    DATA_TILT_SENSITIVITY,
    DATA_LED_TYPE,
    DATA_MAP_JOY_DPAD,
    DATA_MAP_START_SEL_HOME,
    DATA_MAP_ACCEL_RIGHT,
    DATA_PINS,
    DATA_KEYS,
    DATA_THRESHOLD_JOY,
    DATA_THRESHOLD_TRIGGER,
    DATA_THRESHOLD_DRUM,
    DATA_LED_PINS,
    DATA_LED_COLOURS,
    DATA_MIDI_TYPE,
    DATA_MIDI_NOTE,
    DATA_MIDI_CHANNEL,
};